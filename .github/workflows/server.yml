name: 24/7 Cloud Minecraft Engine & Bot

on:
  push:
    branches: [ "main" ] # Automatically boots up whenever you change a file
  workflow_dispatch: # Gives you a manual "Run workflow" button on GitHub web

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository Configuration
        uses: actions/checkout@v4

      - name: Install Runtime Java Environment
        uses: actions/setup-java@v4
        with:
          distribution: 'microsoft'
          java-version: '21'

      - name: Provision Playit Tunnel Agent
        run: |
          sudo curl -o /usr/local/bin/playit https://playit.gg
          sudo chmod +x /usr/local/bin/playit

      - name: Force Download and Extract PaperMC Folder
        run: |
          sudo apt-get update && sudo apt-get install -y unzip
          
          # 📡 FIXED AUTOMATIC LINK RESOLVER: Targets the explicit 'downloadButton' element inside MediaFire's code
          echo "🔍 Fetching raw page layout..."
          PAGE_CONTENT=$(curl -sL "https://mediafire.com")
          
          echo "🔍 Scanning layout for download ID mappings..."
          FINAL_URL=$(echo "$PAGE_CONTENT" | grep -oE 'href="https?://download[^"]+"[[:space:]]+id="downloadButton"' | grep -oE 'https?://[^"]+')
          
          if [ -z "$FINAL_URL" ]; then
            echo "⚠️ Fallback: Running structural pattern scanning..."
            FINAL_URL=$(echo "$PAGE_CONTENT" | grep -oE 'https?://download[^"'\'' ]+' | head -n 1)
          fi
          
          # Clean out hidden layout formatting blocks
          FINAL_URL=$(echo "$FINAL_URL" | tr -d '\r' | tr -d ' ' | tr -d '"')
          echo "📡 Found live file server endpoint: $FINAL_URL"
          
          if [ -z "$FINAL_URL" ]; then
            echo "❌ Critical Error: Could not resolve MediaFire download link element structure."
            exit 1
          fi
          
          # Pulls down your 380MB PaperMc server zip package cleanly
          curl -L -o PaperMC.zip "$FINAL_URL"
          
          if [ -f "PaperMC.zip" ]; then 
            unzip -o PaperMC.zip
            echo "✅ Server files unpacked successfully!"
          else
            echo "❌ Error: Failed to extract server zip archive pack."
            exit 1
          fi

      - name: Initialize Discord Bot & Server Loop
        env:
          PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        run: |
          # 1. Write the bot code into a clean file right inside the cloud machine
          cat << 'EOF' > run_bot.py
          import os
          import discord
          from discord.ext import commands

          intents = discord.Intents.default()
          intents.message_content = True
          bot = commands.Bot(command_prefix="!", intents=intents)

          @bot.event
          async def on_ready():
              print(f"🤖 Cloud Bot is online inside GitHub Actions as: {bot.user}")

          @bot.command()
          async def status(ctx):
              await ctx.send("🟢 **Server Status:** The cloud console is active and PaperMC is running in the background!")

          bot.run(os.getenv("DISCORD_TOKEN"))
          EOF

          # 2. Boot up your Discord bot in the background so it stays online 24/7
          pip install discord.py
          python run_bot.py &
          echo "🚀 Discord control bridge initialized successfully!"

          # 3. Step inside your folder and fire up the main Paper engine
          cd PaperMc
          echo "eula=true" > eula.txt
          playit --secret "$PLAYIT_SECRET" & 
          java -Xmx6G -Xms6G -jar paper.jar nogui
